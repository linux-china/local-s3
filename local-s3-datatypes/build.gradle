plugins {
    id 'java-library'
    id 'local-s3.java-conventions'
    id 'local-s3.publish'
}

dependencies {
    // Types of the public API: the serializers of the model extend Jackson's StdSerializer and ValueSerializer, and
    // take JsonGenerator and SerializationContext.
    api(libs.jacksonDatabind)

    implementation(libs.jacksonDataformatXml)
}
